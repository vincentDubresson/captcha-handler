<puzzle-captcha
        key="{{ challenge }}"
        width="350"
        height="200"
        piece-width="80"
        piece-height="50"
        src="{{ path('captcha', {challenge}) }}"
></puzzle-captcha>